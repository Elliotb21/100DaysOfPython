# Starting code
def odd_or_even(number):
    if number % 2 = 0:
        return "This is an even number."
    else:
        return "This is an odd number."
# Without debugging, we can see the editor is providing us a clue. On line 3, we are wanting to compare values, but 
# instead we are using an assignment operator.
# Running the code, we see, "SyntaxError: cannot assign to expression here. Maybe you meant '==' instead of '='?"
# it even tells us line 3